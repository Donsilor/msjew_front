const getters = {
  cartGoodsNum(state) {
    return state.cartGoodsNum;
  },
  wishGoodsNum(state) {
    return state.wishGoodsNum;
  },
  userInfo(state) {
    return state.userInfo;
  }
};

export default getters;
